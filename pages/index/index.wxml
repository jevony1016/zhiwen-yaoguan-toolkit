<view class="page">
  <view class="hero">
    <image class="brandLogo" src="/assets/logo.png" mode="aspectFit" />
    <text class="eyebrow">知闻药观</text>
    <text class="title">医药工具百宝箱</text>
    <text class="subtitle">当前开放 CTCAE v6.0 不良事件分级检索，更多工具开发中，有好的建议欢迎公众号「知闻药观」后台私聊。</text>
  </view>

  <view class="disclaimer">
    本工具仅用于学习、资料检索与工作参考，不构成医学诊断、治疗建议或临床决策依据。
  </view>

  <view class="sectionHead">
    <text class="sectionTitle">检索工具</text>
  </view>

  <view class="toolList">
    <block wx:for="{{tools}}" wx:key="id">
      <view class="toolCard" data-path="{{item.path}}" bindtap="openTool">
        <view class="toolTop">
          <view class="toolIcon">{{item.icon}}</view>
          <view class="toolText">
            <text class="toolTitle">{{item.title}}</text>
            <text class="toolSubtitle">{{item.subtitle}}</text>
          </view>
        </view>
        <view class="toolFoot">
          <text class="toolMeta">{{item.meta}}</text>
          <text class="toolStatus">{{item.status}}</text>
        </view>
      </view>
    </block>
  </view>
</view>
